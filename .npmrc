# tsdown 0.21 declares `peerOptional typescript@^5 || ^6`; this repo builds on
# TypeScript 7 and drives declaration emit through tsgo explicitly, so the
# unsatisfied peer is expected. Without this, `npm i` and `npm ci` both fail.
legacy-peer-deps=true
