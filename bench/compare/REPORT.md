# apecs vs. the JS/TS ECS field

> Generated by `node report.mjs` from `results.json`. Measured 2026-09-04 on Apple M1, Node v20.19.0 (darwin arm64).

## What is being compared

| Library      | Version             | Design                                                                                                                                    |
| ------------ | ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| hand-written | hand-written        | Typed arrays and index arithmetic. No entity ids, no queries, no structural change.                                                       |
| apecs        | 0.1.0 (dist)        | Archetype + SoA typed columns. `each` is the idiom; `chunks` hands back the raw arrays, `accessor` a resolved field for access by handle. |
| bitECS       | 0.4.0               | Sparse sets; components are user-owned typed arrays indexed by entity id. No archetypes, no ergonomic tier.                               |
| koota        | 0.6.6               | Archetype-ish query cache over SoA stores held in plain JS arrays (not typed arrays).                                                     |
| becsy        | 0.15.5 (perf build) | Archetype + typed columns, but scheduler-first: every figure includes one `world.execute()` frame.                                        |

apecs is measured from its built `dist/` — the same bytes a user installs, with `__DEV__` assertions
compiled out (verified by `npm run check:bundle`). becsy is measured from its `perf` build for the
same reason: its default build carries runtime validation and benchmarking it would be dishonest.

## Method, and the two things that would have made this report wrong

**One process per (library, benchmark).** Loading four ECS libraries into one isolate makes the shared
call sites in the iteration path megamorphic, and V8 then deoptimises whichever library warmed up
second. That effect is larger than most of the differences being measured.

**A fairness census, run before any timing.** Every library must match the _same number of entities_
on every query-shaped benchmark, or the run aborts. This is not a formality — it caught two real
problems that would each have produced a fake result:

- `Not()` in apecs takes exactly one operand, unlike `Or()`, and silently ignored the other twelve
  arguments the adapter passed it. The `mixed_query` benchmark was matching 96 153 entities instead
  of 49 998.
- becsy's `perf` build has its assertions compiled out. On the default `maxShapeChangesPerFrame` it
  silently creates only _some_ of the entities you ask for, and every query built on them comes back
  short — with no error anywhere. Three of becsy's benchmarks were quietly iterating nothing.

Structural benchmarks get the same treatment: `verify-structural.mjs` proves the add/remove work
actually changes state, and because becsy defers structural changes to the frame boundary,
`verify-becsy-coalesce.mjs` proves it is not collapsing an in-frame add+remove into nothing
(3588.8µs in-frame vs 3665µs across alternating frames — it is doing the work).

Every figure is the mitata average over a ≥1s CPU-time sample, after an explicit warmup.

## Head-to-head

Each library at its fastest correct idiom, in microseconds, with the multiple of the hand-written
baseline in brackets. Lower is better; **bold** is the fastest real library on that row.

| Benchmark       | hand-written | apecs             | bitECS         | koota          | becsy            |
| --------------- | ------------ | ----------------- | -------------- | -------------- | ---------------- |
| `packed_1`      | 4.0          | **4.9 (1.2×)**    | 6.3 (1.5×)     | 12.3 (3.0×)    | 22.5 (5.6×)      |
| `packed_5`      | 5.0          | **5.2 (1.0×)**    | 7.7 (1.5×)     | 14.6 (2.9×)    | 29.9 (6.0×)      |
| `simple_iter`   | 123          | 286 (2.3×)        | **170 (1.4×)** | 727 (5.9×)     | 3211 (26.2×)     |
| `frag_iter`     | 109          | **98.6 (0.9×)**   | 120 (1.1×)     | 430 (4.0×)     | 392 (3.6×)       |
| `entity_cycle`  | 255          | **15288 (60.1×)** | 40823 (160.4×) | 77415 (304.1×) | 136075 (534.5×)  |
| `add_remove`    | 132          | 21296 (161.0×)    | 18374 (138.9×) | 27894 (210.9×) | **4176 (31.6×)** |
| `mixed_query`   | 60.5         | **49.6 (0.8×)**   | 64.8 (1.1×)    | 182 (3.0×)     | 195 (3.2×)       |
| `random_access` | 111          | 1626 (14.6×)      | **139 (1.2×)** | 224 (2.0×)     | 806 (7.2×)       |

What each row is measuring:

- **`packed_1`** — 5 000 entities, five traits each, one arithmetic pass over one trait. Best-case iteration. One archetype, no misses, nothing to skip.
- **`packed_5`** — 1 000 entities, five traits each, five passes — one per trait. Per-query fixed cost. Five short scans expose setup overhead a long scan hides.
- **`simple_iter`** — 100 000 entities, Position + Velocity, one integrate pass (SPEC §12.1). The real-world hot loop. This is the number that matters most.
- **`frag_iter`** — 100 000 entities spread over 26 archetypes, iterate the one shared trait. Fragmentation cost. Archetype designs pay per archetype; sparse-set designs do not.
- **`entity_cycle`** — Spawn 100 000 entities with two traits, then despawn all of them. Entity churn: id allocation, row insert, row release, free-list reuse.
- **`add_remove`** — Add a trait to 100 000 existing entities, then remove it from all of them. Structural change. Archetype designs move a row; sparse-set designs flip a bit.
- **`mixed_query`** — 26 archetypes; query the shared trait while excluding 13 of the 26 fragment traits. Negation. Archetype matching resolves it once at query build; per-entity designs re-test every entity.
- **`random_access`** — Read and write one field on 100 000 entities in shuffled order, by entity handle. The archetype tax. Random access is where a row-indirection design should lose to a flat sparse set.

## The two tiers

Most of these libraries offer an ergonomic idiom and a raw escape hatch, and the gap between them is
large enough that quoting one number per library hides the story. bitECS has no ergonomic tier — the
raw arrays are the API. becsy has no raw tier — everything goes through a system.

| Benchmark       | apecs `each` | apecs `chunks` / `accessor` | koota `updateEach` | koota `useStores` | bitECS | becsy  |
| --------------- | ------------ | --------------------------- | ------------------ | ----------------- | ------ | ------ |
| `packed_1`      | 9.3          | 4.9                         | 108                | 12.3              | 6.3    | 22.5   |
| `packed_5`      | 34.6         | 5.2                         | 194                | 14.6              | 7.7    | 29.9   |
| `simple_iter`   | 582          | 286                         | 7206               | 727               | 170    | 3211   |
| `frag_iter`     | 197          | 98.6                        | 2568               | 430               | 120    | 392    |
| `entity_cycle`  | 15288        | –                           | 77415              | –                 | 40823  | 136075 |
| `add_remove`    | 21296        | –                           | 27894              | –                 | 18374  | 4176   |
| `mixed_query`   | 93.9         | 49.6                        | 1145               | 182               | 64.8   | 195    |
| `random_access` | 2819         | 1626                        | 12746              | 224               | 139    | 806    |

becsy's figures each include one `world.execute()` frame, which the others do not pay. Measured on
its own that frame costs 0.4µs — negligible at these scales, so it is not distorting anything.

## Memory

Bytes per entity for a world of entities carrying Position + Velocity — two `f32` fields each, so
**16 bytes is the actual payload**. Everything above 16 is what the library charges for ids, masks,
sparse sets, archetype bookkeeping and query caches.

Measured as absolute resident bytes at five world sizes, least-squares fitted — _not_ as a
before/after delta, which subtracts two heap readings taken at different points in the collector's
life and is dominated by when it happened to compact. Measured that way, 100 000 entities came out
_smaller_ than 10 000. The check that the fit works: the hand-written baseline must land on 16.

| Library      | Bytes/entity | Overhead above payload    |
| ------------ | ------------ | ------------------------- |
| hand-written | 16.0         | — (this _is_ the payload) |
| apecs        | 37.6         | 2.3× payload              |
| bitECS       | 236.7        | 14.8× payload             |
| koota        | 293.8        | 18.4× payload             |
| becsy        | 155.5        | 9.7× payload              |

At 1 000 000 entities that is 38 MB for apecs against 237 MB for bitECS and 294 MB for koota.

## The finding that matters most: the ergonomic tier falls off a cliff at five traits

V8's inline caches hold four shapes before going megamorphic. Every library here that offers a
per-entity ergonomic accessor dispatches through a shared internal call site, and once a program
uses enough distinct traits with it, that site tips over.

ns per entity-visit, ergonomic tier, **one trait-count per process** — sweeping `k` inside a single
process lets earlier shapes pollute the dispatch site, which manufactures the cliff whether or not
`k` causes it:

| distinct traits | 1    | 2    | 3    | 4    | 5    | 6    | 8    |
| --------------- | ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| apecs           | 2.2  | 1.9  | 2.6  | 2.9  | 12.8 | 38.2 | 40.0 |
| bitECS          | 1.6  | 1.5  | 1.5  | 1.5  | 1.5  | 1.5  | 1.6  |
| koota           | 22.0 | 32.5 | 33.3 | 32.7 | 32.6 | 32.8 | 32.5 |
| becsy           | 5.4  | 9.6  | 9.9  | 10.4 | 38.4 | 38.7 | 38.2 |

- **apecs is the fastest ergonomic tier measured below five traits** (1.9–2.9 ns) and the worst hit
  above it: **17× slower at eight traits than at one.** Any real program uses more than four traits.
- **becsy** has the same cliff in the same place, 7×.
- **koota** degrades only 1.5×, but starts an order of magnitude behind.
- **bitECS is immune** — flat at ~1.5 ns. It has no per-component dispatch to go megamorphic.

SPEC §12.2 rule 2 says "no megamorphic call sites in the iteration path: cursor classes are per-trait,
not shared." Per-trait cursor classes are precisely what makes the shared dispatch site polymorphic.
The rule is aiming at the right target and the mechanism defeats it.

**apecs's `chunks` tier is completely immune** and is the fastest thing in this report — flat at
1.01/0.98/1.00/0.99 ns/entity through eight traits, against bitECS's ~1.5.

Per-call costs, for sizing systems that run over small archetypes
(`node scaling.mjs`): `each` is 165 ns fixed + 1.77 ns/entity;
`chunks` is 35 ns fixed + 0.93 ns/entity.

## Capability comparison: sorted queries

Sorting is where apecs claims a feature the field mostly lacks. bitECS and becsy ship none; koota has
`QueryResult.sort()`, which re-sorts on demand. 100 000 entities iterated in sorted order every frame,
microseconds:

|                                    | keys never change | 1% of keys change per frame |
| ---------------------------------- | ----------------- | --------------------------- |
| apecs `sortBy`                     | 3321              | 3034                        |
| koota `sort()`                     | 26321             | 19016                       |
| hand-written `Array.sort` + gather | 1453              | 1404                        |

apecs is **6–8× faster than koota** here, which is the headline. But it is also **2.3× slower than
simply sorting an id array by hand**, and that deserves attention rather than a victory lap:

- Iterating the same query linearly costs 2 ns/entity. Iterating it sorted costs 38.6 ns/entity — **19×**.
- A hand-written gather over an identical precomputed order costs 1.3 ns/entity, so the gap is not memory locality; it is ~37 ns/entity of overhead inside the sorted iteration driver.
- `sorted.entities()` on a view whose keys were **never touched** costs 5.4 ns/entity, and that cost is flat at every world size (5.78 / 5.57 / 5.43 ns/entity at 1 k / 10 k / 100 k). It is O(n).

SPEC §12.1 budgets `sorted-static` at **zero work** — "one `lastWriteTick` compare per matching
archetype." The measured cost is linear in entity count at every size, so that budget is not being met.
This is the clearest actionable gap the comparison turned up.

## Reproducing

```bash
cd bench/compare && npm install
node run.mjs        # census, timings, memory -> results.json
node report.mjs     # results.json + findings.json -> REPORT.md
```

Supplementary measurements in `findings.json` each name the script that reproduces them.
